
// this function is intended to a string's first letter to a capital letter 

export default function capitalizeFirstLetter(string) {
    return string.charAt(0).toUpperCase() + string.slice(1);
}


